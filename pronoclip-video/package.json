{
  "name": "pronoclip-video-core",
  "version": "0.5.0-dev",
  "private": true,
  "type": "module",
  "description": "Cœur logique (core/) et adaptateurs (adapters/) du plugin pronoclip-video.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "example": "tsx scripts/generate-example.ts",
    "prompts": "tsx scripts/generate-prompts.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
